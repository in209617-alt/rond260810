# 그래픽 리소스 교체 가이드

게임 화면에 나오는 그림은 전부 `assets/` 폴더의 PNG 파일이에요.
**같은 이름, 같은 위치로 덮어쓰면** 게임 그림이 바뀌어요. 코드는 고칠 필요 없어요.

전체 목록은 `assets_preview.png`에서 한눈에 볼 수 있어요. 4배로 확대한 그림이고, 회색 체크무늬 부분은 투명한 영역이에요.

## 공통 규칙

- 형식은 **PNG**, 배경은 **투명**이에요. 타일(`tiles/`)은 가득 채워도 돼요.
- 파일 이름은 **영어 소문자 그대로** 써야 해요. `Tree_1.png`, `tree 1.png`처럼 바뀌면 못 찾아요.
- 도트 그래픽이라 게임이 그림을 **정수 배율(2배, 3배…)로 크게 확대**해서 보여 줘요. 원본은 표에 적힌 **작은 픽셀 크기 그대로** 그리면 돼요.
- 파일이 없거나 이름이 틀리면 게임에 들어 있는 기본 그림이 대신 나와요. 원하는 것만 하나씩 바꿔도 게임이 멈추지 않아요.
- 도트 툴 추천: [Aseprite](https://www.aseprite.org)(유료), [Piskel](https://www.piskelapp.com)(무료, 웹), [Libresprite](https://libresprite.github.io)(무료)

## 1. 바닥 타일 `assets/tiles/` — 모두 **16 × 16 px**

타일은 16 × 16 칸에 딱 맞춰 그려져요. 크기가 다르면 16 × 16으로 늘이거나 줄여서 그려지니 꼭 16 × 16으로 만들어 주세요.

| 파일 | 설명 |
| --- | --- |
| `grass_1.png` ~ `grass_4.png` | 기본 잔디. 칸마다 4장 중 하나를 골라 깔아서 반복이 덜 보여요. **네 장의 가장자리가 서로 자연스럽게 이어지게** 그려 주세요. |
| `grass_dark_1.png`, `grass_dark_2.png` | 조금 짙은 잔디. 맵 곳곳에 얼룩처럼 깔려요. |
| `path_1.png`, `path_2.png` | 흙길 |
| `water.png` | 연못 물 (가장자리 쪽) |
| `water_deep.png` | 연못 물 (가운데 깊은 쪽) |
| `path_edge_top/bottom/left/right.png` | **덮개(투명 배경).** 흙길 위에 덮어서, 옆 칸이 잔디일 때 풀이 길 쪽으로 삐져나온 모양을 만들어요. `top`이면 그림의 위쪽 가장자리에 풀을 그려요. |
| `shore_top/bottom/left/right.png` | **덮개(투명 배경).** 물 위에 덮어서, 옆 칸이 땅일 때 물가 테두리를 만들어요. |

## 2. 장식 `assets/decor/` — 모두 **16 × 16 px, 투명 배경**

잔디 칸 위에 한 칸 크기로 덮어 그려져요. 밟고 지나갈 수 있어요.

| 파일 | 설명 |
| --- | --- |
| `flower_white.png`, `flower_pink.png`, `flower_purple.png`, `flower_yellow.png` | 꽃 무리 |
| `mushroom.png` | 버섯 |

## 3. 오브젝트 `assets/objects/` — 크기 자유, 투명 배경

**그림의 아래쪽 가운데가 오브젝트가 서 있는 칸의 바닥 가운데**에 맞춰져요. 나무라면 줄기 밑동이 그림 맨 아래 가운데에 오게 그려 주세요. 크기는 자유예요. 나무를 더 크게 그리면 게임에서도 더 크게 나와요.

부딪히는 범위는 그림 크기와 상관없이 **밑동 한 칸의 아래쪽 좁은 영역**으로 정해져 있어요. 그래서 캐릭터가 나무 뒤로 지나가면 잎에 가려지는 효과가 자동으로 생겨요.

| 파일 | 지금 크기 | 설명 |
| --- | --- | --- |
| `tree_1.png` | 34 × 48 | 활엽수 |
| `tree_2.png` | 34 × 48 | 활엽수 (색 변형) |
| `tree_apple.png` | 34 × 48 | 사과나무 |
| `pine.png` | 30 × 50 | 소나무 |
| `bush.png` | 20 × 16 | 덤불 |
| `bush_berry.png` | 20 × 16 | 열매 덤불 |
| `rock.png` | 16 × 12 | 바위 |
| `stump.png` | 16 × 13 | 그루터기 |
| `sign.png` | 16 × 18 | 표지판 |

그림자는 오브젝트 그림에 넣지 마세요. `effects/shadow.png`가 발밑에 자동으로 깔려요.

## 4. 캐릭터 `assets/characters/` — 스프라이트 시트

| 파일 | 설명 |
| --- | --- |
| `player1.png` | 1P 캐릭터 (지금은 빨간 셔츠) |
| `player2.png` | 2P 캐릭터 (지금은 파란 셔츠) |

**가로 4칸 × 세로 4줄**로 된 한 장짜리 그림이에요. 지금은 한 칸이 16 × 24 px라서 전체 64 × 96 px예요.

```
         칸1      칸2        칸3      칸4
줄1 아래  서 있음  왼발 내딛음  서 있음  오른발 내딛음
줄2 위    〃       〃          〃       〃
줄3 왼쪽  〃       〃          〃       〃
줄4 오른쪽 〃      〃          〃       〃
```

- 걸을 때 칸1 → 2 → 3 → 4 순서로 반복되고, 멈추면 칸1만 보여요.
- **칸 크기는 바꿔도 돼요.** 게임이 그림 전체를 가로 4, 세로 4로 똑같이 나눠서 한 칸 크기를 계산해요. 예를 들어 한 칸 20 × 32로 그리려면 전체 80 × 128로 만들면 돼요.
- 각 칸의 **아래쪽 가운데가 발밑**이에요. 네 줄 모두 발 위치를 같은 높이에 맞춰 주세요.
- 이름표는 칸 높이에 맞춰 머리 위에 자동으로 떠요.

## 5. 효과 `assets/effects/`

| 파일 | 지금 크기 | 설명 |
| --- | --- | --- |
| `shadow.png` | 32 × 8 | 캐릭터·오브젝트 발밑 그림자. 반투명 타원으로 그리면 돼요. 오브젝트 너비에 맞춰 자동으로 늘어나요. |
| `water_sparkle.png` | 3 × 1 | 연못 위에서 반짝였다 사라지는 빛 |
| `leaf_1.png`, `leaf_2.png` | 2 × 2 | 화면 위에서 떨어지는 나뭇잎 (초록, 주황) |

## 6. 아이템 아이콘 `assets/items/` — 모두 **16 × 16 px, 투명 배경**

인벤토리·상점에 보이는 그림이에요. 파일 이름은 `js/items.js`의 아이템 id와 같아야 해요.

| 파일 | 아이템 |
| --- | --- |
| `weed.png` | 잡초 |
| `berry.png` | 베리 |
| `apple.png` | 사과 |
| `mushroom.png` | 버섯 (맵에 있는 버섯은 `decor/mushroom.png`) |
| `ribbon.png` | 리본 |
| `glasses.png` | 안경 |
| `shirt.png` | 줄무늬 셔츠 |
| `overalls.png` | 멜빵바지 |

## 7. 입은 모습 `assets/equipment/` — 캐릭터 시트와 **똑같은 크기·배치**

옷을 입으면 캐릭터 위에 겹쳐 그려지는 그림이에요. 캐릭터 시트처럼 **가로 4칸 × 세로 4줄**(아래, 위, 왼쪽, 오른쪽)이고, 같은 칸끼리 정확히 겹쳐져요.
그래서 **캐릭터를 새로 그리면 옷 그림도 새 캐릭터에 맞춰 다시 그려야** 해요. 지금 옷 그림은 짧은 2등신 캐릭터(64 × 96)에 맞춰져 있어요.

- 옷 부분만 그리고 나머지는 투명하게 두세요. 캐릭터 그림 위에 그대로 덮여요.
- 겹치는 순서: 상의 → 바지 → 머리 → 장신구 (뒤에 있을수록 위에 그려져요)
- 파일 이름은 아이템 id와 같아요: `ribbon.png`, `glasses.png`, `shirt.png`, `overalls.png`

## 8. UI 아이콘 `assets/ui/` — **16 × 16 px, 투명 배경**

| 파일 | 설명 |
| --- | --- |
| `coin.png` | 코인 |
| `bag.png` | 가방 버튼 |
| `trash.png` | 쓰레기통 |

## 새 아이템 추가하기

1. `js/items.js`의 `ITEMS`에 한 줄 추가해요. 예: `hat: { name: "밀짚모자", kind: "wear", slot: "head", stack: 1, buy: 15, sell: 7, desc: "여름용 모자." }`
2. 아이콘 `assets/items/hat.png`(16 × 16)를 그려요.
3. 입는 아이템이면 `assets/equipment/hat.png`(캐릭터 시트와 같은 크기)도 그려요.
4. `buy` 가격을 적으면 상점 구매 목록에 자동으로 나와요.

## 바꾼 그림 올리기

1. 그린 PNG를 같은 이름으로 저장해요.
2. GitHub 저장소에서 해당 폴더로 들어가요. 예: `assets` → `objects`
3. **Add file → Upload files**로 파일을 끌어다 놓고 **Commit changes**를 눌러요. 같은 이름이면 자동으로 덮어써져요.
4. 1~2분 뒤 게임을 새로고침해요.

**예전 그림이 계속 보이면:** 브라우저가 옛 그림을 기억하고 있는 거예요. `js/assets.js` 파일의 `ASSET_VERSION = 1` 숫자를 `2`, `3`…으로 하나씩 올려서 같이 올리면 모든 플레이어에게 새 그림이 바로 보여요.

**올리기 전에 미리 보기:** 로컬 서버(`python3 -m http.server 8000`)로 열어 보면 GitHub에 올리기 전에 내 컴퓨터에서 확인할 수 있어요. 브라우저 콘솔(F12)에 `[forest] 기본 그림을 쓴 리소스: …`가 뜨면 그 파일은 이름이나 위치가 틀려서 못 찾은 거예요.
